# SubFix v3.3.0

- 修复字幕音量边界调整导致句首靠后、句尾提前结束的问题，保护原始文字对齐范围。
- 连续字幕保留原始交接点，避免后处理制造空隙；保留原有长停顿。
- 字幕显示延长改为时间处理的最后一步，防止后续调整再次截短字幕。
- 本地 Qwen 与豆包云端识别共用修复后的 v5 时间边界处理，无需重新下载模型。

完整安装：下载 `SubFix-v3.3.0-Full-macOS.zip`，解压后打开 PKG。已安装用户可在插件内使用“检查更新”，更新后重新打开插件并重新生成字幕。已有字幕不会自动改动。

验证：63 项自动化测试通过；209 条字幕缓存回放中，后处理截短句尾由 189 条降为 0，新增空隙由 152 处降为 0，原有长停顿保留。此结果验证后处理修复，不代表模型原始时间戳完全无误；未另行完成本地 Qwen 全流程实测。

支持 Apple Silicon Mac。沿用未签名 PKG；完整包包含 Python、FFmpeg、强制对齐程序与模型。本地 Qwen 识别依赖和识别模型仍在首次使用时下载。

## SubFix v3.2.9

- AI 工作台统一使用纯文字任务菜单，将中英互译合并为“中英翻译”，新增“简繁转换”。
- 翻译和简繁转换自动判断整批字幕的转换方向，移除方向下拉框，修复控件重叠；无法可靠判断时保留原字幕并提示重试。
- 简繁转换保留字幕时间、行序、标点、空格、英文和数字，只转换字形，不替换地区用语。
- 本地 Qwen 依赖使用清华 PyPI 镜像，模型优先魔搭 ModelScope，失败后尝试 Hugging Face；兼容已有环境和模型。
- 在线更新包补齐后台进程管理助手，与完整安装包保持一致。
- 保留 v3.2.8 的后台安装响应、用户目录环境及卸载应用修复。

完整安装：下载 `SubFix-v3.2.9-Full-macOS.zip`，完整解压后打开 PKG。已安装用户可使用插件内“检查更新”。支持 Apple Silicon Mac；沿用未签名 PKG，卸载应用使用本地签名，未进行 Apple 公证。完整包包含 Python、FFmpeg、强制对齐程序和模型；本地 Qwen 识别依赖与识别模型仍在首次使用时下载。

## SubFix v3.2.6

- 口播、现场、单段和批量生成统一使用 v5，默认正式应用 v5 调整，不再自动降级到旧引擎。
- 修复不同说话人的字幕边界调整后可能重叠的问题。
- 检查更新不再弹出进度窗口；确认更新后显示下载进度。
- 修复本地 Qwen 依赖安装：先升级 pip，再下载依赖，支持重试和断点恢复；改善安装失败提示与首次加载检查。
- 修复安装完成阶段的脚本错误。
- 本版本源码和发行配置移除私人字幕样本、项目与时间线诊断信息，保留 v5 算法参数。

## 下载选择

- 完整安装：SubFix-v3.2.6-Full-macOS.zip，解压后打开其中的 pkg。
- 在线更新：已安装用户可在插件中选择“检查更新”；SubFix-update-v3.2.6.zip 是供更新器使用的文件。

支持 Apple Silicon Mac，安装包未签名。完整包包含 Python、FFmpeg、字幕强制对齐程序与对齐模型；本地 Qwen 语音识别依赖和识别模型仍在首次使用时联网下载。

隐私范围：本版本使用独立的脱敏源码快照。旧分支、旧标签、旧安装包及他人已经下载的副本不因此被清除。
